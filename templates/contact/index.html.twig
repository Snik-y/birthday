{% extends 'base.html.twig'%}
{% block body %}
    <h1>Hello {{firstname}}</h1>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Send">
    {{ form_end(form) }}
{% endblock body %}